// A library is a collection of routines in a programming language. Dart offers a set of built-in libraries that can be used to store commonly used routines.
import 'dart:math';

void main() {
  int i = 9;
  print(sqrt(i));
}
