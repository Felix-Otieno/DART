// Returns the product of two integers. Takes two parameters and returns an int.
void main() {
  print(findProduct(4, 5));
}

int findProduct(int num1, int num2) {
  int product = num1 * num2;
  return product;
}
